<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!--
            Postgres, not sqlite: several migrations use Postgres-only DDL
            (partial unique indexes, DROP CONSTRAINT on the default pkey name)
            to enforce per-tenant uniqueness, so the test suite has to run
            against the real engine. A dedicated database keeps it isolated
            from the dev data in ntcsdb — create it once with:
              docker compose exec postgres psql -U ntcsuser -d postgres \
                -c "CREATE DATABASE ntcsdb_testing OWNER ntcsuser;"
        -->
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_HOST" value="postgres"/>
        <env name="DB_PORT" value="5432"/>
        <env name="DB_DATABASE" value="ntcsdb_testing"/>
        <env name="DB_USERNAME" value="ntcsuser"/>
        <env name="DB_PASSWORD" value="ntcspassword"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TENANCY_CENTRAL_DOMAINS" value="localhost,127.0.0.1,ntcsweb.test"/>
        <env name="TENANCY_ROOT_DOMAIN" value="ntcsweb.test"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
